import { $ } from './element';
import { setStyle, setStyles } from './style';
import {
  addEvent, addEvents,
  removeEvent, removeEvents,
  onTap,
} from './event';
import { getClientXY } from './position';

export {
  $,
  setStyle, setStyles,
  addEvent, addEvents,
  removeEvent, removeEvents,
  onTap,
  getClientXY,
};